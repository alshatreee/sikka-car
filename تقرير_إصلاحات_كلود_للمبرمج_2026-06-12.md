# تقرير تسليم للمبرمج — إصلاحات أمان مالي على البوتات

**التاريخ:** 12 يونيو 2026
**المُنفِّذ:** مراجعة آلية (Claude) بطلب المالك
**الفرع:** `claude/polymarket-hedge-fund-setup-wlHTt`
**كوميت الإصلاحات:** `bef55ef` (الأب المباشر `4480786` = النسخة قبل الإصلاح)
**الملفات المعدَّلة:** `vps_ready/bot_monitor.py` ، `vps_ready/channel_daytrader.py`
**الحجم:** ‎+60 / −20 سطر (تعديلان فقط، بلا تغييرات معمارية)

> **الغرض:** هذه إصلاحات سلامة على بوتات **تتداول بمال حقيقي**. كلها مُتحقَّق منها بالكود الفعلي (لا من تقرير)، ومُنشَرة على السيرفر، ومُختبَرة حيّة. الرجاء مراجعتها ورائي.

---

## كيف تراجع كل التعديلات بنفسك

```bash
git fetch origin
# كل تعديلاتي مقابل النسخة قبلها:
git diff 4480786 bef55ef -- vps_ready/bot_monitor.py vps_ready/channel_daytrader.py
# أو ملف الكوميت:
git show bef55ef
```

النسخة الحيّة على السيرفر (`/root/bots/`) = `4480786` + هذه التعديلات بالضبط (تأكّدنا بـ`diff` أنه لا يوجد أي تعديل آخر).

---

## ملخص التعديلات (10)

| # | الملف | الخطورة | المشكلة باختصار |
|---|------|---------|-----------------|
| 1 | bot_monitor | 🔴 عالٍ | أمر «بيع ربح» لا يعمل لعملات channel_daytrader (خطأ مفتاح) |
| 2 | channel_daytrader | 🔴 عالٍ | فلتر اتجاه BTC يفشل «مفتوحاً» (يشتري أثناء هبوط BTC عند فشل API) |
| 3 | bot_monitor | 🔴 عالٍ | احتمال بيع مزدوج عند انقطاع الشبكة بعد إرسال الأمر |
| 4 | bot_monitor | 🟡 متوسط | فحص حد $1 يتم قبل تقريب الكمية |
| 5 | bot_monitor | 🟡 متوسط | `_fetch_coin_balance` قد يُرجع رصيداً مقفلاً (walletBalance) |
| 6 | channel_daytrader | 🟡 متوسط | تصادم dedup عند `msg_id=0` |
| 7 | channel_daytrader | 🟡 متوسط | مقارنة زمنية بـ`replace(tzinfo)` بدل `astimezone` |
| 8 | bot_monitor | 🟢 تحصين | `_execute_profit_sell` لا يفحص `BYBIT_SECRET` |
| 9 | bot_monitor | 🟢 تحصين | `float()` على مدخل أمر «رأس مال» قد يرمي استثناء |
| 10 | bot_monitor | 🟢 تحصين | واجهة الأوامر تعمل حتى لو `TELEGRAM_CHAT_ID` فارغ |

---

## التفاصيل (before / after)

### 1) 🔴 أمر «بيع ربح» معطّل لعملات channel_daytrader — `bot_monitor.py` (`get_all_positions`)

**المشكلة:** `channel_daytrader.py` يكتب سعر الدخول في ملف الحالة تحت المفتاح `entry`، بينما `bot_monitor` كان يقرؤه `entry_price`. النتيجة: كل مراكز هذا البوت تُقرأ بسعر دخول = 0، فتُحذف من `فرص`، وأمر `بيع ربح SYM` يرد «لم أجد سعر دخول».

```python
# قبل
"entry": pos.get("entry_price", 0),
# بعد
# channel_daytrader writes the entry under "entry" (not "entry_price").
"entry": pos.get("entry", pos.get("entry_price", 0)),
```

**الأثر:** أمر «بيع ربح» وقائمة «فرص» يعملان الآن لعملات channel_daytrader.
**للمراجعة:** تأكّد من مفتاح الكتابة في `channel_daytrader.py` (دالة فتح المركز) — يكتب `"entry"` فعلاً. (daytrading_bot يكتب `entry_price` وهو سليم.)

---

### 2) 🔴 فلتر اتجاه BTC يفشل مفتوحاً — `channel_daytrader.py`

**المشكلة:** الكاش الافتراضي `"up": True`. عند فشل API لشمعات BTC، تُرجع الدالة `True` → يُسمح بالشراء أثناء هبوط BTC فعلي. يناقض إصلاح `_coin_trend_up` (fail-closed) المعتمد سابقاً.

```python
# قبل
_btc_trend_cache = {"ts": 0.0, "up": True}
# بعد
# Fail closed: default to "not an uptrend" so a BTC API failure blocks buys
# instead of allowing them during an unknown/possibly-down market.
_btc_trend_cache = {"ts": 0.0, "up": False}
```

**الأثر:** فشل جلب اتجاه BTC يمنع الشراء بدل السماح به.
**ملاحظة للمراجعة (لم أغيّرها):** الدالة `btc_trend_up()` تُبقي آخر قيمة ناجحة 15 دقيقة وتُرجعها حتى لو فشل التحديث لاحقاً. الإصلاح يعالج أسوأ حالة (أول تشغيل/فشل دائم). لو أردت تشدّداً أكثر: أعِد القيمة `False` عند أي فشل تحديث، لا فقط في الافتراضي.

---

### 3) 🔴 خطر البيع المزدوج عند انقطاع الشبكة — `bot_monitor.py` (`_bybit_signed_post` + دالتا البيع)

**المشكلة:** عند استثناء شبكي (مثل timeout بعد إرسال الطلب) كانت الدالة تُرجع `None`، فتظهر للمستخدم «❌ فشل البيع» رغم أن الأمر **قد يكون نُفِّذ فعلاً** على Bybit. إعادة الإرسال → بيع مرتين. لا idempotency.

```python
# بعد — إضافة orderLinkId + سنتينل «غير مؤكد»
params = dict(params)
params.setdefault("orderLinkId", f"mon{int(time.time()*1000)}{_secrets.token_hex(3)}")
...
except Exception as e:
    log(f"Bybit POST AMBIGUOUS (order may have executed): {e}")
    return {"retCode": -999, "retMsg": "network error", "_ambiguous": True,
            "orderLinkId": params.get("orderLinkId", "")}
```

وفي `_execute_sell` و `_execute_profit_sell` قبل فحص النجاح:

```python
if result and result.get("_ambiguous"):
    return ("⚠️ انقطع الاتصال أثناء بيع ... — قد يكون الأمر نُفِّذ!\n"
            "❗️لا تُعد الإرسال. تحقق من الرصيد في Bybit أولاً (أمر «رصيد»).\n"
            f"orderLinkId: {result.get('orderLinkId','')}")
```

أُضيف `import secrets as _secrets` أعلى الملف.

**الأثر:** انقطاع الشبكة لم يعد يظهر كـ«فشل نظيف»؛ يُحذَّر المستخدم صراحةً من إعادة الإرسال.
**اقتراح تطوير (لم يُنفَّذ — قرارك):** idempotency حقيقي بإعادة استخدام نفس `orderLinkId` عند المحاولة الثانية + استعلام `GET /v5/order/realtime?orderLinkId=...` قبل السماح بإعادة المحاولة. يتطلب تخزين نيّة الأمر — تركته لك.

---

### 4) 🟡 فحص حد $1 قبل التقريب — `bot_monitor.py` (`_execute_sell` و `_execute_profit_sell`)

**المشكلة:** كان فحص `value_usd < 1.0` يتم على الكمية **قبل** تقريبها لـ lot step؛ بعد التقريب لأسفل قد تنزل القيمة تحت $1 فيُرسَل أمر مخالف.

```python
# بعد — التقريب أولاً ثم الفحص
step = _get_lot_step(pair)
sell_qty = _round_qty(sell_qty, step)
if sell_qty <= 0:
    return "❌ الكمية صفر بعد التقريب ..."
value_usd = sell_qty * price          # يُحسب بعد التقريب
if value_usd < 1.0:
    return "❌ قيمة البيع أقل من $1 ..."
```

(نفس الترتيب طُبّق في `_execute_profit_sell`.)
**الأثر:** لا يُرسَل أمر قيمته الفعلية < $1.

---

### 5) 🟡 رصيد مقفل — `bot_monitor.py` (`_fetch_coin_balance`)

**المشكلة:** السلسلة الاحتياطية كانت تصل إلى `walletBalance` الذي يشمل المبالغ المقفلة في أوامر مفتوحة → قد يُرجع رصيداً غير قابل للبيع.

```python
# قبل
for fld in ("availableToWithdraw", "free", "walletBalance"):
# بعد
# Only spendable balance. walletBalance includes funds locked in
# open orders — using it could oversell or double-sell.
for fld in ("availableToWithdraw", "free"):
```

**الأثر:** البيع يعتمد على الرصيد القابل للإنفاق فقط.
**ملاحظة:** `_fetch_usdt_balance` ما زال يستخدم `walletBalance/equity` للعرض فقط (لا للبيع) — تركته كما هو.

---

### 6) 🟡 تصادم dedup عند `msg_id=0` — `channel_daytrader.py` (`open_from_signal`)

**المشكلة:** مفتاح الـdedup `f"{channel}:{msg_id}"`؛ عند `msg_id=0` (رسائل بلا معرّف) تتصادم كل الإشارات على `channel:0`.

```python
# بعد
if sig.msg_id:
    sig_key = f"{sig.channel}:{sig.msg_id}"
else:
    digest = hashlib.md5(f"{sig.channel}:{sig.symbol}:{sig.raw_text[:80]}".encode()).hexdigest()[:12]
    sig_key = f"{sig.channel}:h{digest}"
```

**الأثر:** الإشارات بلا معرّف لم تعد تتصادم على مفتاح واحد. (`hashlib` و`raw_text` موجودان أصلاً.)

---

### 7) 🟡 مقارنة زمنية — `channel_daytrader.py` (`scan_channels`)

```python
# قبل
if msg.date and msg.date.replace(tzinfo=timezone.utc) < since:
# بعد
if msg.date and msg.date.astimezone(timezone.utc) < since:
```

**السبب:** `msg.date` من Telethon واعٍ بالمنطقة الزمنية؛ `replace` يستبدل الميتاداتا دون تحويل، بينما `astimezone` يحوّل صحيحاً. وقائي ضد فلترة عمر خاطئة للإشارات.

---

### 8) 🟢 تحصين — `_execute_profit_sell` يفحص السر — `bot_monitor.py`

```python
# قبل:  if not BYBIT_KEY:
# بعد:  if not BYBIT_KEY or not BYBIT_SECRET:
```

(مطابقة لـ`_execute_sell`.)

---

### 9) 🟢 تحصين — مدخل «رأس مال» — `bot_monitor.py` (`_handle_command`)

```python
# بعد
try:
    dep = float(cap_match.group(1))
    wd = float(cap_match.group(2)) if cap_match.group(2) else 0
except ValueError:
    return "❌ مبلغ غير صالح (مثال: رأس مال 1000 200)"
```

يمنع استثناء `ValueError` (مثل «1.2.3») من بلوغ حلقة الأوامر.

---

### 10) 🟢 تحصين — حارس `TELEGRAM_CHAT_ID` فارغ — `bot_monitor.py` (`_command_loop`)

```python
# بعد — في بداية الحلقة
if not NOTIFY_CHAT:
    log("CMD: TELEGRAM_CHAT_ID غير مضبوط — واجهة الأوامر معطّلة")
    return
```

fail-closed: لا تُشغّل واجهة التداول إن لم يُضبط معرّف المحادثة المصرّح.

---

## أمور تحقّقت أنها سليمة (لم تُغيَّر)

- **حساب «بيع الربح فقط» صحيح رياضياً:** بيع نسبة `(السعر−الدخول)/السعر` يسحب الربح نقداً ويُبقي رأس المال مستثمراً (تحقّقت بمثال: دخول $1، حالي $1.5 → 33.3%).
- **فلتر السكور في daytrading_bot:** البوابة المبكرة 50، والنهائية **62** بعد memory boost قائمة (`daytrading_bot.py`). لا تمر عملة دون تجاوز 62 فعلياً.
- **وقف الخسارة حقيقي 2%** على بوتَي التداول اليومي (الـ«999%» في تقارير سابقة يخص البوت الشهري غير المُعدَّل).
- **بوابة الأمان (chat_id)** تعمل، ولا مفاتيح API مكتوبة في الكود.

## بنود لم تُعالَج (قرارك)

| البند | الملف | لماذا تُرك |
|-------|------|-----------|
| `_scan_profitable_coins` يعرض الربح على الرصيد الحي الكامل | bot_monitor | عرض/تقرير فقط، لا ينفّذ بيعاً خاطئاً |
| نافذة الإيداعات/السحوبات 6 أشهر | bot_monitor | يؤثر على رقم رأس المال في `رصيد` فقط |
| trailing يتخطّى فحص SL وانتهاء المهلة أثناء تفعيله | daytrading_bot | غير ضار: trail بعد التفعيل دائماً أعلى من SL؛ ملاحظة لا أكثر |
| لا تأكيد قبل البيع بالسوق | bot_monitor | تصميم؛ يمكن إضافة «أكّد بيع X» خلال 30 ثانية لو رغبت |
| idempotency كامل للأوامر | bot_monitor | يتطلب تخزين نيّة الأمر (انظر بند #3) |

---

## النشر والتحقق

- النسخة الحيّة في `/root/bots/` نُزّلت من رأس الفرع وتُطابق `bef55ef`.
- `python3 -m py_compile` ناجح للملفين.
- bot_monitor خدمة systemd `bot-monitor.service`؛ channel_daytrader عبر nohup + `watchdog.sh` (cron).
- اختُبرت واجهة الأوامر حيّة (ردّ على `حالة`).

### ملاحظات تشغيلية ظهرت أثناء النشر (للعِلم — ليست من تعديلاتي)
1. **`monthly-channel-bot.service` مخفية (masked)** والبوت الشهري يعمل عبر nohup بنسخة واحدة. كود `run_checks` يتجاهل الخدمات المخفية → **لا خطر تداول مزدوج**. لكن `build_health_report` (أمر `حالة`) يعرضها «متوقف (dead)» لأنه لا يميّز masked عن crashed — **خلل عرض تجميلي فقط**. (تحسين اختياري: إظهار «مُدار عبر nohup».)
2. نسخة `bot_monitor.py.bak.*` الناتجة من النشر خرجت **0 بايت** (غالباً curl سابق أفرغ الملف قبل النسخ). للرجوع للنسخة قبل الإصلاح استعمل GitHub بالـSHA `4480786` بدل ملف الـbak.
3. ظهر `TG getUpdates: HTTP Error 409: Conflict` بسبب نسخة bot_monitor شبحية ثانية تنازعت على توكن تيليجرام — حُلّت بقتل الشبح وإبقاء نسخة systemd الوحيدة.

---

## ملاحظة عن مطابقة الكود

- **channel_daytrader:** نسخة السيرفر قبل النشر طابقت GitHub بايتاً ببايت → الإصلاح على عين الكود الذي كان يعمل (مؤكد).
- **bot_monitor:** الملف الحي الآن = `4480786` + هذه الإصلاحات بالضبط (مؤكد بـ`diff`). تعذّر إثبات تطابق نسخة السيرفر القديمة بسبب الـbak الفارغة؛ يُغلق نهائياً بتأكيد المالك أنه لم يُعدّل `bot_monitor.py` يدوياً على السيرفر خارج GitHub.
